<%- include('../partials/head', { pageTitle: title }) %>
<main class="min-h-screen flex items-center justify-center px-4">
  <div class="bg-white rounded-2xl shadow-sm border p-10 text-center max-w-md w-full">
    <div class="w-16 h-16 mx-auto rounded-full bg-emerald-100 text-emerald-600 flex items-center justify-center text-3xl mb-4">✓</div>
    <h1 class="text-2xl font-bold text-slate-900 mb-2"><%= title %></h1>
    <p class="text-slate-600"><%= message %></p>
    <p class="text-xs text-slate-400 mt-6">이 창은 닫으셔도 됩니다.</p>
  </div>
</main>
</body></html>
